package CRUD_app.view;

import CRUD_app.model.Label;
import CRUD_app.model.Post;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.PrintWriter;

public class PostView {

//    public Post gettingDataToPost(BufferedReader reader, PrintWriter out) throws IOException {
//        out.println("Добавить пост? (да/нет):");
//        if ("да".equals(reader.readLine())) {
//            Post post = new Post();
//
//            out.println("Введите ID поста:");
//            post.setId(Integer.parseInt(reader.readLine()));
//            out.println("Введите заголовок поста:");
//            post.setTitle(reader.readLine());
//            out.println("Введите содержимое поста:");
//            post.setContent(reader.readLine());
//            Label label;
////            while (((label = gettingDataToLabel()) != null)) {
////                post.addLabel(label);
////            }
//            return post;
//        }
//        return null;
//    }

}
